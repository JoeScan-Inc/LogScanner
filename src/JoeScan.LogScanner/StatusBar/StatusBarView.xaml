<UserControl
    x:Class="JoeScan.LogScanner.StatusBar.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="45"
    d:DesignWidth="1200"
    mc:Ignorable="d">
    <DockPanel LastChildFill="False">
        <TextBox
            x:Name="BuildInfo"
            MinWidth="160"
            Margin="3"
            DockPanel.Dock="Right"
            IsReadOnly="True" />
        <Label VerticalContentAlignment="Center" DockPanel.Dock="Right">Build:</Label>
        <TextBox
            x:Name="Adapter"
            MinWidth="160"
            Margin="3"
            DockPanel.Dock="Right"
            IsReadOnly="True" />
        <Label VerticalContentAlignment="Center" DockPanel.Dock="Right">Adapter:</Label>
        <TextBox
            x:Name="EngineUnits"
            MinWidth="160"
            Margin="3"
            DockPanel.Dock="Right"
            IsReadOnly="True" />
        <Label VerticalContentAlignment="Center" DockPanel.Dock="Right">Units:</Label>
        <ContentControl DockPanel.Dock="Right" x:Name="EncStatus"></ContentControl>
    </DockPanel>
</UserControl>
